name: Build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Install NPM Dependencies
      run: npm install
    - name: Build
      run: npm run build -- all
    - name: Build solid package
      run: npm run build-solid
